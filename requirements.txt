# Core dependencies
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.5,<3
python-dotenv>=1.0,<2

# AI/Agent orchestration (InferenceClientModel uses HF Inference API over HTTP — no torch needed)
smolagents>=1.14,<2
huggingface_hub>=0.30,<1

# HTTP clients
requests>=2.32,<3
httpx>=0.27,<1

# Async support
aiohttp>=3.10,<4

# Browser automation (used by browser_tools.py)
playwright>=1.40,<2

# Misc
python-multipart>=0.0.9,<1
